/// The db level lock manager for managing table level 2 phase locking.
#[derive(Debug, Default)]
pub struct LockManager {
    // Something Something Nothing Nothing
}

impl LockManager {
    pub fn new() -> Self {
        Self::default()
    }
}
